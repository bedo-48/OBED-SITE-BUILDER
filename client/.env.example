# Base URL of the better-auth server (used by the auth client)
VITE_BETTER_AUTH_URL=http://localhost:3000

# Base URL of the backend API (axios). Usually the same host as the server.
VITE_API_URL=http://localhost:3000
